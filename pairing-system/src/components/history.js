import React, { Component } from 'react'
import PropTypes from 'prop-types'

class history extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                <h1>history test test</h1>
            </div>
        )
    }
}
export default history;